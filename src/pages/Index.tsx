import { WallCalendar } from "@/components/WallCalendar";

const Index = () => {
  return (
    <main className="min-h-screen bg-background py-6 sm:py-10 md:py-14">
      <WallCalendar />
    </main>
  );
};

export default Index;
